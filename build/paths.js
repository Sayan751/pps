const base = "./";
const paths = {
    tsConfig: base + "tsconfig.json",
    tsLintConfig: base + "tslint.json",
    src: base + "src",
    dist: base + "dist",
    test: base + "test"
};
exports.paths = paths;